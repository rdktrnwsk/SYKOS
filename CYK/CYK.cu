#include "CYK.cuh"

template<int action>
__global__ void cykAlgorithm(curandState * randGlobal)
{
	/*if (action == 1 || action == 2) {
		printf("%d", threadIdx.x);
	}
	*/
	printf("heheh");

	return;
}